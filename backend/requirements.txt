# PrintReady AI - dipendenze backend
# ------------------------------------------------------------------
# Nucleo web / API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27
websockets>=12.0

# Elaborazione mesh
numpy>=1.26
scipy>=1.11
trimesh>=4.0
manifold3d>=2.3        # riparazione/booleane robuste (STL manifold)
networkx>=3.2
shapely>=2.0
pillow>=10.2
lxml>=5.0                # lettura/scrittura 3MF
rtree>=1.2              # indice spaziale: ray casting, autointersezioni, prossimità
fast-simplification>=0.1.7  # decimazione QEM compilata (fallback interno se assente)
embreex>=2.17           # ray tracing Intel Embree: ~1000x sul controllo spessori
scikit-image>=0.22      # marching cubes del generatore locale (esiste una riserva a voxel)

# Opzionali (attivati automaticamente se presenti)
# open3d>=0.18         # ricostruzione superfici, filtri avanzati
# pymeshlab>=2023.12   # decimazione qualità pro
# torch>=2.2           # accelerazione GPU CUDA per i modelli locali
# cupy-cuda12x         # accelerazione GPU per operazioni numeriche
